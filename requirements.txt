# MCP server
mcp>=1.2.0

# Gemini API — current SDK (google-genai replaces the deprecated google-generativeai)
google-genai>=1.0.0

# Env var loading
python-dotenv>=1.0.1

# HTTP bridge (POST /live_compare for the dashboard demo)
fastapi>=0.110.0
uvicorn>=0.29.0

# Live research: fetch official URL + parse HTML
requests>=2.32.0
beautifulsoup4>=4.12.0
